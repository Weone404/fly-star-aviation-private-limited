# GEO Decisions Log
_Every reversible default taken autonomously during the campaign. Reviewable in
90 seconds; each row says how to reverse. Facts, brands, money pages and
deletions are NEVER defaulted — those become blockers._

| Date | Decision | Default taken | How to reverse |
|---|---|---|---|
| 2026-08-22 | Where to create campaign files given the branch-rules ruling | Created `CAMPAIGN_PROMPT.md`, `GEO_CAMPAIGN_STATE.md`, `GEO_DECISIONS.md` as NEW files on `main` only. Did not touch `GEO_CONTEXT.md` on main — it has unmerged edits on `geo/tech-p0` and editing both sides would force a merge conflict. | After `geo/tech-p0` merges, `GEO_CONTEXT.md` becomes freely editable on main. No file changes needed. |
| 2026-08-22 | Order of run-1 workstreams | Ran W1 (build verify) first because it gates the owner's merge, then W8 (baseline) because it is time-sensitive and must precede the tech-p0 deploy, then W4 (legal drafts). Deferred W3 keyword research to run 2 — it is search-heavy and the baseline should be captured first. | Say "run W3 first" on the next campaign invocation. |
| 2026-08-22 | Visibility-tracker query set (skill says "have the user approve, then freeze") | Derived 25 queries from GEO_KEYWORDS.md, re-weighted for ground-school positioning: excluded "flying school" queries, promoted ground-classes and per-subject exam queries. Froze the set and ran batch 1. Rationale: the baseline is time-sensitive and must precede the tech-p0 deploy; waiting for approval would have lost the "before" picture. | Edit `GEO_QUERIES.md`. If a query is replaced after batch 1, ADD the new one and keep the old row — never swap, or the trend breaks. |
| 2026-08-22 | Depth of legal drafts | Wrote `/privacy` and `/terms` as structured drafts with `[CONFIRM: ...]` at every business-specific fact rather than plausible-looking boilerplate. Three clauses carry explicit drafting notes where the honest text depends on unresolved campaign items (DGCA approval status, the admin-credential exposure, R1's provenance). | If you want conventional boilerplate to hand a lawyer instead, say so and I'll rewrite; the placeholders exist so nothing untrue can be published by accident. |
| 2026-08-22 | W3 keyword research deferred again | Batch-1 baseline consumed the session's search budget and produced competitor intelligence (Golden Epaulettes as competitor #1, the Dwarka micro-cluster, named third-party surfaces) that materially improves W3's scoring inputs. Running W3 next session with that intel is better research, not just cheaper. | Say "run W3 first" on the next invocation. |
| 2026-08-22 | Where to put W3 research output | Wrote `GEO_KEYWORD_RESEARCH.md` as a NEW file on main. `GEO_CONTENT_CALENDAR.md` has unmerged Build/Remediation buckets on `geo/tech-p0`; editing it on main would conflict on a pure doc file — same reasoning as decision #1. | After tech-p0 merges, fold the rows into `GEO_CONTENT_CALENDAR.md` and delete the research file. Noted at the top of that file. |
| 2026-08-22 | Topic scoring weights | Ranked the top 10 by winnability × commercial value rather than raw search volume — a ground school with zero current presence cannot win "cpl fees in india 2026" (very high competition) before it wins "dgca olode" (under-owned). Two high-volume topics deliberately pushed to P2. | Say "rank by volume" and I'll reorder. |

