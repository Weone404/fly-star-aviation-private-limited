# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*
flystar-routine-prompt.txt
blog-gate-report.json

# Local API snapshots. contacts-snapshot.json holds real people's names, phone
# numbers and email addresses — it must never be committed, and it does not
# belong on a shared machine.
blog-snapshot.json
contacts-snapshot.json
*-snapshot.json
